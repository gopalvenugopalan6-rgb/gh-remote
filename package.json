{
  "name": "gh-remote",
  "version": "2.0.0",
  "description": "Browser remote for LG webOS TVs. Static site, no server, no account.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "node dev/mock-tv.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "ws": "^8.18.0"
  }
}
